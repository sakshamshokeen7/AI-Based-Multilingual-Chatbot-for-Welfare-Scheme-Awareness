document.addEventListener('DOMContentLoaded', () => {
    const ctaButton = document.querySelector('.cta-button');

    ctaButton.addEventListener('click', () => {
        alert('Live Demo starting... (This will integrate with the Twilio sandbox link in the future)');
    });

    console.log("Welfare Scheme Chatbot Dashboard loaded.");
});
